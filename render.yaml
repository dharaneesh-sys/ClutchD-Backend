services:
  - type: web
    name: clutchd-api
    runtime: python
    region: singapore
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && python scripts/bootstrap_db.py 2>&1 && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: clutchd-db
          property: connectionString
      - key: SYNC_DATABASE_URL
        fromDatabase:
          name: clutchd-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: clutchd-redis
          property: connectionString
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: http://localhost:3000,https://clutchd-app.onrender.com
      - key: DEBUG
        value: "false"
      - key: PYTHONPATH
        value: /opt/render/project/src/backend
      - key: DEV_AUTO_VERIFY_PROVIDERS
        value: "false"

  - type: keyvalue
    name: clutchd-redis
    plan: free
    ipAllowList: []

databases:
  - name: clutchd-db
    plan: free
