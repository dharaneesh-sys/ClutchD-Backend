[Unit]
Description=Tailscaled: always restart when killed (ClutchD SSH lifeline)
# Stock unit uses Restart=on-failure, which does NOT restart after a
# manual kill/crash with a clean exit. SSH to this box rides Tailscale,
# so a dead tailscaled = unreachable server. Always restart.

[Service]
Restart=always
RestartSec=3
